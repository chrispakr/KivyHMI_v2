#:import utils kivy.utils

# <NavButtonLf@Button>:
#     #size_hint: [None, None]
#     #size: [184,60]
#     background_color: (0,1,0,0)
#     font_name: "StdFont"
#     font_size: 16
#     color: utils.get_color_from_hex("#858585")
#     canvas.before:
#         Color:
#             rgb: (utils.get_color_from_hex("#151515") if self.state == "normal" else utils.get_color_from_hex("#3b3b3b"))
#         RoundedRectangle:
#             pos: self.pos
#             size: self.size
#             radius: [4,]
#         Color:
#             rgb: utils.get_color_from_hex("#858585")
#         Line:
#             width: 1.05
#             rounded_rectangle: self.x, self.y, self.width, self.height, 4, 50


<ScreenLoadFilm>:
    on_enter:
        #app.sendMoveCommand(60)
        app.toolbar_disabled = True
        app.startFilmLoad()
    canvas.before:
        Color:
            rgb: utils.get_color_from_hex("#3b3b3b")
        Rectangle:
            pos: self.pos
            size: self.size

    NavButton:
        text: "<< CANCEL"
        pos: 8,8
        size_hint: [None, None]
        size: 184, 60
        on_press:
            app.screenBackward('screenHome')
            app.stopFilmLoad()

    NavButton:
        text: "FILM FFW >>"
        pos: 416,8
        size_hint: [None, None]
        size: 184, 60
        on_press: app.enable_filmLoadFastForward()
        on_release: app.disable_filmLoadFastForward()
            

    NavButton:
        text: "Init-Film >>"
        disabled: app.curMoveCommand != 0
        pos: 608,8
        size_hint: [None, None]
        size: 184, 60
        on_press:
            app.screenForward("screenInitFilm")
            app.startFilmInit()